services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: weaponguid
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - weaponguid_pg:/var/lib/postgresql/data

volumes:
  weaponguid_pg:
